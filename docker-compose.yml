services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: smarttodo
      POSTGRES_PASSWORD: smarttodo_dev
      POSTGRES_DB: smarttodo
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
